let oneNumber = "20";
let twoNumber = "4";


function checkString (oneNumber, twoNumber) {
    let remains = oneNumber % twoNumber;
    if(remains===0) {
        console.log("остачі не має")
    }
        else { console.log(`остача :${remains}`)}
    }

    checkString (oneNumber, twoNumber)