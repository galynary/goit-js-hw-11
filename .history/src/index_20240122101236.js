let string = "риои";

function checkString (string) {
    if(string.length >= 1) {
        console.log(string.length-1)
    }
        else { console.log("довжина рядка не має більша 2 символів")}
    }

    checkString (string);