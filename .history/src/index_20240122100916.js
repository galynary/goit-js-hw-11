let string = "g";

function checkString (string) {
    if(string.length >= 1) {
        console.log("довжина рядка має більша 2 символів")
    }
        else {"довжина рядка  не має більше 2 символів"}
    }

    checkString (string);